"use client";
import React from 'react';
import MobileMenu from "@/components/Menu";

const HomePage = () => {
    return (
        <div>
            <MobileMenu />
        </div>
    );
};

export default HomePage;